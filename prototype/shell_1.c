#include <stdio.h>
#include <stdlib.h>/* import exit */
#include <unistd.h>/* */
#include <sys/wait.h>/* import wait */
#include <sys/type.h>/* import pid_t */
/**
 * main - creates a simple shell
 *
 * Return: always 0.
 */
int main()
{
	/* 1. get the line */

	/* 2. chop it up to pieces  so to have the different args */

	/* 3. create a child process to execute of it */

	/* execute */

	return (0);
}
